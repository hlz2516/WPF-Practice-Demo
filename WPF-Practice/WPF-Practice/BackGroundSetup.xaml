<UserControl x:Class="WPF_Practice.BackGroundSetup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Practice"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="btn" TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="MinHeight" Value="35" />
            <Setter Property="Margin" Value="4 0 4 0" />
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button x:Name="btn_apply" Content="应用" Style="{StaticResource btn}" />
            <Button x:Name="btn_add" Content="添加" Style="{StaticResource btn}" />
            <Button x:Name="btn_del" Content="删除" Style="{StaticResource btn}" />
            <Button x:Name="btn_reset" Content="重置" Style="{StaticResource btn}" />
        </StackPanel>
        <ScrollViewer>
            <WrapPanel>
                
            </WrapPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
